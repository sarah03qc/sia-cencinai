# ORDEN DE INSTALACIÓN — no instalar con un solo `pip install -r requirements.txt`
# de corrido sin fijarte en esto:
#   1. torch (versión exacta de abajo)
#   2. autoawq
#   3. transformers, forzando de nuevo la versión exacta (autoawq lo baja de versión
#      como dependencia transitiva al instalarse, así que hay que reinstalarlo después):
#        pip install torch==2.6.0
#        pip install autoawq
#        pip install transformers==4.51.3 --force-reinstall --no-deps
#   4. el resto de este archivo, en cualquier orden

torch==2.6.0

# Extracción de texto de PDFs
pypdf>=4.0

# Embeddings (BGE-M3)
sentence-transformers>=3.0

# Vector store
faiss-cpu>=1.8

# Carga y ejecución de los LLMs
transformers==4.51.3
bitsandbytes>=0.43
accelerate>=0.33
autoawq          # cuantización AWQ, para Llama 3.3 70B. Instalar ANTES de forzar
                  # la versión de transformers de arriba (ver orden al inicio del
                  # archivo) — autoawq downgradea transformers como dependencia.
# Manejo del dataset (benchmark_300.xlsx)
pandas>=2.2
openpyxl>=3.1

# Métricas de evaluación (preguntas abiertas)
bert-score>=0.3.13
rouge-score>=0.1.2
nltk>=3.9

# Utilidades numéricas usadas directamente en rag/
numpy>=1.26
